# Rust / Tauri
target/
src-tauri/target/
**/*.rs.bk
*.pdb

# Build output
dist/

# Config générée à l'exécution (les valeurs par défaut sont dans le code)
config.json
settings.json

# ... mais la config Claude Code du projet, elle, se versionne : c'est une décision
# (quels plugins sont coupés ici, et pourquoi), pas un fichier généré.
!.claude/settings.json

# Windows
Thumbs.db
desktop.ini

# Généré par tauri-build
**/gen/schemas/
